torch>=2.2.0
numpy>=1.26.0

# 이미지 처리
opencv-python>=4.9.0
Pillow>=10.0.0
scikit-image>=0.22.0   # Sauvola 이진화

# OCR 엔진
paddlepaddle>=2.6.0    # CPU: paddlepaddle, GPU: paddlepaddle-gpu
paddleocr>=2.7.0       # 한국어 인식률 최고 (PP-OCRv4)
pytesseract>=0.3.10    # Tesseract Python 래퍼 (Tesseract 바이너리 별도 설치 필요)

# Detection
craft-text-detector>=0.4.2   # CRAFT 텍스트 검출

# 유틸
tqdm>=4.66.0
